version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./frpc.toml:/app/frpc.toml
      - ./app_config.json:/app/app_config.json
    network_mode: "host"
    restart: always
